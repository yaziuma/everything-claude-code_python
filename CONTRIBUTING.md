# Everything Claude Codeへの貢献

貢献していただき、ありがとうございます。このリポジトリはClaude Codeユーザーのためのコミュニティリソースとなることを意図しています。

## 求めているもの

### エージェント

特定のタスクを上手く処理する新しいエージェント：
- 言語固有レビュアー（Python、Go、Rust）
- フレームワーク専門家（Django、Rails、Laravel、Spring）
- DevOps専門家（Kubernetes、Terraform、CI/CD）
- ドメイン専門家（MLパイプライン、データエンジニアリング、モバイル）

### スキル

ワークフロー定義とドメイン知識：
- 言語ベストプラクティス
- フレームワークパターン
- テスト戦略
- アーキテクチャガイド
- ドメイン固有知識

### コマンド

有用なワークフローを呼び出すスラッシュコマンド：
- デプロイメントコマンド
- テストコマンド
- ドキュメントコマンド
- コード生成コマンド

### フック

有用な自動化：
- リンティング/フォーマットフック
- セキュリティチェック
- バリデーションフック
- 通知フック

### ルール

常に従うべきガイドライン：
- セキュリティルール
- コードスタイルルール
- テスト要件
- 命名規則

### MCP設定

新しいまたは改良されたMCPサーバー設定：
- データベース統合
- クラウドプロバイダーMCP
- 監視ツール
- コミュニケーションツール

---

## 貢献方法

### 1. リポジトリをフォーク

```bash
git clone https://github.com/YOUR_USERNAME/everything-claude-code.git
cd everything-claude-code
```

### 2. ブランチを作成

```bash
git checkout -b add-python-reviewer
```

### 3. 貢献を追加

適切なディレクトリにファイルを配置：
- `agents/` 新しいエージェント用
- `skills/` スキル用（単一.mdまたはディレクトリ）
- `commands/` スラッシュコマンド用
- `rules/` ルールファイル用
- `hooks/` フック設定用
- `mcp-configs/` MCPサーバー設定用

### 4. 形式に従う

**エージェント**にはフロントマターが必要：

```markdown
---
name: agent-name
description: 何をするか
tools: Read, Grep, Glob, Bash
model: sonnet
---

指示をここに...
```

**スキル**は明確で実行可能であるべき：

```markdown
# スキル名

## 使用タイミング

...

## 動作方法

...

## 例

...
```

**コマンド**は何をするかを説明すべき：

```markdown
---
description: コマンドの簡潔な説明
---

# コマンド名

詳細な指示...
```

**フック**には説明を含めるべき：

```json
{
  "matcher": "...",
  "hooks": [...],
  "description": "このフックが何をするか"
}
```

### 5. 貢献をテスト

提出前にClaude Codeで設定が動作することを確認してください。

### 6. PRを提出

```bash
git add .
git commit -m "Pythonコードレビューエージェントを追加"
git push origin add-python-reviewer
```

その後、以下を含むPRを開いてください：
- 何を追加したか
- なぜ有用なのか
- どのようにテストしたか

---

## ガイドライン

### すべきこと

- 設定を集中的でモジュラーに保つ
- 明確な説明を含める
- 提出前にテストする
- 既存のパターンに従う
- 依存関係を文書化する

### すべきでないこと

- 機密データ（APIキー、トークン、パス）を含める
- 過度に複雑またはニッチな設定を追加する
- テストしていない設定を提出する
- 重複する機能を作成する
- 代替手段なしに特定の有料サービスを要求する設定を追加する

---

## ファイル命名

- ハイフン付き小文字を使用：`python-reviewer.md`
- 説明的に：`tdd-workflow.md`（`workflow.md`ではなく）
- エージェント/スキル名をファイル名に合わせる

---

## 質問？

イシューを開くか、X：[@affaanmustafa](https://x.com/affaanmustafa)でお問い合わせください

---

貢献していただき、ありがとうございます。一緒に素晴らしいリソースを構築しましょう。